// ============================================================
// CRIATIVOS (ANÚNCIOS) — WESLEY MOTOS
// Os nomes vêm direto do CSV da Meta, não precisa cadastrar
// manualmente. O sistema puxa automaticamente na importação.
// ============================================================

export const VIDEOS = [];

export const VIDEOS_MAP = Object.fromEntries(VIDEOS.map(v => [v.id, v]));
